# Client (Expo) environment. Copy to ".env".
# Base URL of the backend. Use your computer's LAN IP (not localhost) when
# testing on a physical phone, e.g. http://192.168.1.203:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
